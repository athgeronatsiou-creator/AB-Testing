{
  "name": "ab-testing",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "heroku-postbuild": "npm ci --prefix backend --include=dev && npm run build --prefix backend",
    "start": "node backend/dist/index.js"
  }
}
